name: Test EGI-STAT
on: [workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Hello World
        run: echo "✅ GitHub Actions funziona! $(date)"
      - name: Check Secrets
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
          DB_HOST: ${{ secrets.DB_HOST }}
        run: |
          echo "GH_PAT length: ${#GH_PAT:-0}"
          echo "DB_HOST: ${DB_HOST:0:20}***"
          echo "Secrets OK - Pronto per ingest!"
